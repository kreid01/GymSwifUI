mutation postMessage($input: MessageInput!) {
  postMessage(input: $input) {
    id
    content
    date
    user
  }
}

mutation PostChannel($input: ChannelInput!) {
  postChannel(input: $input) {
    id
    name
  }
}

mutation DeleteChannel($id: ID!) {
  deleteChannel(id: $id)
}
